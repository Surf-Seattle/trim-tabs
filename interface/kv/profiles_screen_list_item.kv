
<SurfListItem>
    size_hint_y: None
    height: dp(80)

    canvas:
        Color:
            rgba:
                self.theme_cls.divider_color
        Line:
            points: (root.x+dp(16), root.y, root.x+self.width-dp(16)-0, root.y)
    canvas.before:
        Color:
            rgba: gch("#EDEDED")
        Rectangle:
            size: self.size
            pos: self.pos

    on_release: self.show_dialogue()

    MDBoxLayout:
        orientation: 'vertical'
        adaptive_height: True
        pos_hint: {"center_x": .5, "center_y": .5}
        spacing: dp(2)
        padding: dp(15)

        SurfLabel:  # PROFILE NAME
            text: root.name
            theme_text_color: "Custom"
            text_color: gch("#9C0000")
            font_style: "H4"

    MDBoxLayout:
        orientation: 'horizontal'
        pos_hint: {"center_x": .5, "center_y": .5}
        adaptive_height: True
        padding: "10dp"
#        adaptive_size: True
        spacing: "10dp"

        SurfLabel:
            id: PORT
            text: str(int(root.port_value))
            pos_hint: {"center_x": .5, "center_y": .5}
            halign: "right"
            font_style: "H4"
            theme_text_color: "Custom"
            text_color: gch("#9C0000")

        SurfLabel:
            id: CENTER
            text: str(int(root.center_value))
            pos_hint: {"center_x": .5, "center_y": .5}
            halign: "center"
            font_style: "H4"
            theme_text_color: "Custom"
            text_color: gch("#9C0000")

        SurfLabel:
            id: STARBOARD
            text: str(int(root.starboard_value))
            pos_hint: {"center_x": .5, "center_y": .5}
            halign: "left"
            font_style: "H4"
            theme_text_color: "Custom"
            text_color: gch("#9C0000")

        MDRoundFlatButton:
            id: activate_button
            text: "START"
            pos_hint: {"left": 1, "center_y": 0.5}
            on_release: root.activate()
            md_bg_color: gch("#10ab05")